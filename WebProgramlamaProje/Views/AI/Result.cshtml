@model string

@{
    ViewData["Title"] = "Analiz Sonucu";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-10">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2 class="fw-bold text-primary"><i class="bi bi-stars"></i> Analiz Sonucu</h2>
                <a asp-action="Index" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left"></i> Yeni Analiz
                </a>
            </div>

            <div class="card shadow-lg border-0">
                <div class="card-body p-5">
                    <div class="markdown-content">
                        @* 
                           Not: Güvenlik için Html.Raw kullanırken dikkatli olunmalıdır. 
                           Gemini'den gelen içerik genellikle güvenlidir ancak production ortamında 
                           bir Markdown kütüphanesi (Markdig gibi) kullanılması önerilir.
                           Şimdilik basit bir replace ile satır sonlarını <br> yapıyoruz.
                        *@
                        
                        @if (!string.IsNullOrEmpty(Model))
                        {
                            // Basit Markdown benzeri görüntüleme için
                            var formattedText = Model
                                .Replace("**", "") // Kalın yazıları temizle veya CSS ile hallet
                                .Replace("\n", "<br>"); 
                            
                            // Daha iyi bir görünüm için Markdig kütüphanesi önerilir.
                            // Şimdilik ham metni pre-wrap ile gösterelim, Markdown yapısını korusun.
                            <div style="white-space: pre-wrap; font-family: system-ui; line-height: 1.6;">@Model</div>
                        }
                        else
                        {
                            <div class="alert alert-warning">Sonuç görüntülenemedi.</div>
                        }
                    </div>
                </div>
                <div class="card-footer bg-light p-4 text-center">
                    <p class="mb-0 text-muted small">
                        <i class="bi bi-info-circle"></i> Bu sonuçlar yapay zeka tarafından üretilmiştir ve tıbbi tavsiye niteliği taşımaz. 
                        Lütfen bir uzmana danışmadan ağır egzersizlere başlamayın.
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>
